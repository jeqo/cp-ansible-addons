[Unit]
Description=Prometheus Server
After=network.target

[Service]
User={{prometheus_user}}
Group={{prometheus_group}}
Type=simple
ExecStart={{prometheus_home}}/prometheus-{{prometheus_version}}.linux-amd64/node_exporter

[Install]
WantedBy=multi-user.target